import ExploreViewContainer from "./components/ExploreViewContainer";

/**
 * @component ExplorePage PageComponent
 * @description url 주소에 explore가 들어갈 렌더링 되는 페이지 컴포넌트
 * @returns {JSX.Element} ExplorePage 컴포넌트
 */
const ExplorePage = () => {
    return (
        <div>
            <ExploreViewContainer />
        </div>
    );
};

export default ExplorePage;
